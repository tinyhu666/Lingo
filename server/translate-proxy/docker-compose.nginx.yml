services:
  translate-proxy:
    build:
      context: .
    container_name: lingo-translate-proxy
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    ports:
      - "127.0.0.1:8787:8787"
